# Optional: load hosted UI (login works immediately; no LAN dev server needed)
# EXPO_PUBLIC_WEB_URL=https://ap-sevces.vercel.app

# Default dev: LAN UI on :5500 + API at https://ap-sevces.onrender.com/api
# Redeploy backend on Render after pulling latest server.js (CORS fix for phone LAN IP).
