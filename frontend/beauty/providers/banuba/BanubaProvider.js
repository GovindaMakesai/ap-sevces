/**
 * Banuba BeautyProvider adapter (PLACEHOLDER).
 * Commercial SDK integration occurs in init()/render() — UI does not change.
 * @module beauty/providers/banuba/BanubaProvider
 */
export { BanubaProvider } from '../stubs.js';
