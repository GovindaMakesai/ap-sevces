import { MpaTabPage } from '@/components/MpaTabPage';

/** Production profile-tab.html — hero, wallet, cream menu. */
export function ProfilePage() {
  return <MpaTabPage htmlPath="/profile-tab.html" routePrefix="/profile" title="Profile" />;
}
