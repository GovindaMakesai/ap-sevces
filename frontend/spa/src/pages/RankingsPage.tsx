import { MpaTabPage } from '@/components/MpaTabPage';

/** Production rankings.html — gold leaderboard chrome. */
export function RankingsPage() {
  return <MpaTabPage htmlPath="/rankings.html" routePrefix="/rankings" title="Rankings" />;
}
